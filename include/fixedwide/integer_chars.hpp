#pragma once

/// \file
/// Compatibility shim: the wide-integer parse and format entry points moved
/// into `<fixedwide/chars.hpp>`, which this includes. Include that directly.

#include <fixedwide/chars.hpp>
