# Chosen to match the code that was already here, so adopting it was a
# whitespace-only change rather than a rewrite: 4-space indent, attached braces,
# pointers bound to the type. ColumnLimit is 120 because the existing 99th
# percentile line is 115 characters and the tables of `if constexpr` branches
# read worse when wrapped.
---
Language: Cpp
BasedOnStyle: LLVM

ColumnLimit: 120
IndentWidth: 4
TabWidth: 4
UseTab: Never
AccessModifierOffset: -4

PointerAlignment: Left
ReferenceAlignment: Left
DerivePointerAlignment: false

AllowShortFunctionsOnASingleLine: Inline
AllowShortIfStatementsOnASingleLine: WithoutElse
AllowShortLoopsOnASingleLine: true
AllowShortCaseLabelsOnASingleLine: true
AllowShortBlocksOnASingleLine: Empty

BreakBeforeBraces: Attach
AlwaysBreakTemplateDeclarations: Yes
BreakConstructorInitializers: BeforeColon
PackConstructorInitializers: NextLine

# The public headers document every declaration with /// comments; reflowing
# them would fight the hand-wrapped prose.
ReflowComments: false
SortIncludes: Never

# Explicit-instantiation macros. Without these clang-format reads each as an
# expression and re-indents them into a cascade -- and does it differently on
# every run, so the formatting check never converged. Naming them makes each one
# a statement on its own line.
StatementMacros:
  - FIXEDWIDE_INSTANTIATE_BOTH
  - FIXEDWIDE_INSTANTIATE_128
  - FIXEDWIDE_INSTANTIATE_64
  - FIXEDWIDE_INSTANTIATE_PARSE

SpaceAfterTemplateKeyword: false
SpacesBeforeTrailingComments: 1
IndentPPDirectives: None
